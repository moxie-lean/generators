sudo: false
language: php
php:
  - 5.4

before_install:
  - cd wp-content/plugins/<%= name %>/

install:
  - composer self-update;
  - composer install --no-dev;

cache:
  directories:
  - wp-content/plugins/<%= name %>/vendor

script:
  - composer ci
